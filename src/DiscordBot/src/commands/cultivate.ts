import { SlashCommandBuilder } from 'discord.js';

export const cultivateCommand = {
  data: new SlashCommandBuilder()
    .setName('cultivate')
    .setDescription('Enter the Cultivation World'),
};
