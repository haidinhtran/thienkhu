<RULE[AGENTS.md]>
# Workspace AI Rules

- Do not put any credentials (passwords, tokens, API keys, connection strings, etc.) into documentation files. Create placeholders or templates instead of real/actual credentials, even if they are only for local development or testing.
- Always response to user in Vietnamese but use English in documentation and implementation.
- Always do self-verification after changes and keep related documents up to date.
- When in planning mode, ALWAYS include documentation synchronization and updates as a MUST in the implementation plan.
</RULE[AGENTS.md]>
